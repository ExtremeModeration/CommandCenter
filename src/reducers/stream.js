
const initialState = {
  viewers: [
    {displayName: 'ExtremeModeration'}
  ],
  title: 'Building something interesting',
  playing: 'Creative',
  online: true
}

export default function reducer(state = initialState, action) {
  switch(action.type) {
    default:
      return state
  }
}
